# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare plain text files you want to always be normalized and converted
# to native line endings on checkout.
*.json text eol=lf
*.md text eol=lf